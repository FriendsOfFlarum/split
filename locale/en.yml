flagrow-split:
    forum:
        post:
            was_split: "{count} posts where split {target}."
        post_controls:
            split_button: Split from here
        post_footer:
            split_button: Split to here
